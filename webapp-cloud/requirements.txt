# SoloScribe on Streamlit Community Cloud — Linux, CPU only, Python 3.10.
#
# This is space/requirements.txt minus everything the free tier cannot carry,
# plus streamlit. Guitar isolation (torch + demucs, ~500 MB and minutes of
# CPU) is deliberately absent: the web app runs with separation off and says
# so in its interface. The reasoning for every other line — including why
# setuptools<81 is load-bearing and why numpy/scipy/matplotlib are unpinned —
# is documented at length in space/requirements.txt in the main repository;
# it applies here verbatim.

basic-pitch[onnx]==0.4.0
setuptools<81

librosa==0.11.0
soundfile==0.14.0
pretty_midi==0.2.11.post0
mido==1.3.3
mir_eval==0.8.2
PyGuitarPro==0.11
numpy
scipy
matplotlib

streamlit>=1.40
